import Timetable from '../models/timetable.model.js';

export const createTimetable = async (req, res) => {
  res.send("createTimetable")
};

export const getTimetables = async (req, res) => {
    res.send("getTimetables")
};
